package nz.strydom.gross.interfaces;


public interface IGrossService {

	/*
	 * TODO Add
	 * All prices of 
	 * Find cheapest store for a specific product (will need to return at least the price)
	 * Prices of 
	 * Find cheapest store for a list of products (will need to return 
	 * 
	 */
}
